{
  "kind": "improved",
  "text": "Self-hosted instances can now close sign-ups (REGISTRATION_ENABLED=false) while always allowing the first account, login guessing is throttled per account even across IPs, and every page ships stricter browser security headers."
}
