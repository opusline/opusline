[PHP]
; Read by the static FrankenPHP binary, which scans its cwd (base_path) and
; ignores the Sail php.ini (CLI only). Keep above the 20 MB upload validation.
upload_max_filesize = 21M
post_max_size = 25M
