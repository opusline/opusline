{
  "$schema": "https://www.schemastore.org/package.json",
  "name": "@opusline/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "sh scripts/dev.sh",
    "test": "sh scripts/php.sh sh -c 'php artisan config:clear --quiet && php artisan test --parallel'",
    "test:coverage": "sh scripts/php.sh sh -c 'php artisan config:clear --quiet && php artisan test --parallel --coverage-clover=coverage/clover.xml'",
    "check-types": "sh scripts/php.sh php vendor/bin/phpstan analyse --memory-limit=512M --no-progress",
    "lint": "sh scripts/php.sh sh -c 'vendor/bin/pint --test --parallel --cache-file=.cache/pint.json && vendor/bin/rector process --dry-run'",
    "generate": "sh scripts/php.sh php artisan scramble:export --path=openapi.json"
  },
  "license": "AGPL-3.0-only"
}
