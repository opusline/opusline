{
  "$schema": "https://turborepo.dev/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "description": "Bundles the application with Vite",
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "!**/*.stories.{tsx,jsx,mdx}"],
      "outputs": ["dist/**", "storybook-static/**"]
    },
    "build:storybook": {},
    "//#format-and-lint": {
      "description": "Repo-wide biome check; uncached because root-task hashing misses workspace files",
      "cache": false
    },
    "//#format-and-lint:fix": {
      "cache": false
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "web#dev": {
      "cache": false,
      "persistent": true,
      "with": ["api#dev"],
      "dependsOn": ["@opusline/api-client#generate"]
    },
    "web#build": {
      "description": "Bundles the web app after regenerating the API client",
      "dependsOn": ["^build", "@opusline/api-client#generate"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**"]
    },
    "api#generate": {
      "description": "Exports the OpenAPI spec from the Laravel app",
      "outputs": ["openapi.json"]
    },
    "@opusline/api-client#generate": {
      "description": "Generates the typed client from the OpenAPI spec",
      "dependsOn": ["api#generate"],
      "outputs": ["src/**"]
    },
    "test": {
      "description": "Runs the package's test suite"
    },
    "check-types": {
      "description": "Type-checks the package without emitting output"
    },
    "lint": {
      "description": "Non-mutating format and refactor-drift checks for the package"
    }
  }
}
