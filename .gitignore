# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
node_modules
.pnp
.pnp.js

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Testing
coverage

# Turbo
.turbo

# Vercel
.vercel

# Build Outputs
.next/
out/
build
dist

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Misc
.DS_Store
*.pem

# Scratch PDFs at the root: receipts and invoices dropped here to try the
# parser against real documents. Third-party data in a public repository, one
# `git add -A` away from being published. Committed PDFs live under a directory.
/*.pdf

# Where scripts/opusline-backup.sh writes when it is run from the repository
# root. Each archive holds a verbatim .env — APP_KEY, DB_PASSWORD, S3 keys.
/backups/

# IDEs
.vscode/
.idea/

# Claude Code (machine-written per-user permissions)
.claude/settings.local.json
*.tsbuildinfo
